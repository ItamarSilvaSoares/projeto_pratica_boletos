export enum StatusCodes {
  Unauthorized = 401,
  OK = 200,
  Create = 201,
  BadRequest = 400,
  Conflict = 409,
  NoContent = 204,
  NotFound = 404
}

export enum KeyOptional {
  Cell = 'cell',
  Email = 'email',
  Username = 'username',
  Password = 'password'
}
